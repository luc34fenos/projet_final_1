<!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
      <div class="container">
      	<%= link_to "E-commerce", root_path, class:"navbar-brand" %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav ml-auto">
          	<% if signed_in %>
            <li class="nav-item">
            	<%= link_to "profil", user_path(current_user.id), class:"nav-link" %>
            </li>
            <li class="nav-item">
              	<%= link_to "voir le panier", panier_path(current_user.id), class:"nav-link" %>
            </li>
            <li class="nav-item">
              	<%= link_to "se deconnecter", logout_path , method: :delete, class:"nav-link" %>
            </li>
            <% else %>
            <li class="nav-item">
              	<%= link_to "se connecter", log_path, class:"nav-link" %>
            </li>
            <li class="nav-item">
            	<%= link_to "s'inscrire", new_user_path, class:"nav-link" %>
            </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>


<!-- <%= link_to "voir les articles", root_path %>
<% if signed_in %>
	<%= link_to "profil", user_path(current_user.id) %>
	<%= link_to "voir le panier", panier_path(current_user.id) %>
	<%= link_to "se deconnecter", logout_path , method: :delete %>
<% else %>
	<%= link_to "se connecter", log_path %>
	<%= link_to "s'inscrire", new_user_path %>
<% end %> -->