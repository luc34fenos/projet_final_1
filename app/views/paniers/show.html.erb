<h2>Votre panier</h2>

<% if flash[:error] || flash[:notice] %>
  	<script type="text/javascript">alert("<%= flash[:error] %><%= flash[:notice] %>")</script>
<% end %>
<% @command.each do |c| %>

<ul>
	
		<li><%= Post.find(c.post_id).title %> <spam class="badge badge-info"><%= c.nbre_items %></span>
		<spam class="badge badge-info"><%= c.price %></span>
		</li>
	
</ul>


	<p><%= link_to "effacer", c, method: :delete %></p>
<% end %>
<p class="font-weight-bold">Montant Total:<span class="badge badge-info"> Ar <%= @price %></span></p>
<% if @price != 0 %>
	<%= link_to "acheter", achat_path(@price), data: {confirm: "voulez vous faire cette achat"} %>
<% end %>